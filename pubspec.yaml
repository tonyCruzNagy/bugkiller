name: bugkiller
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  appwrite: ^9.0.1
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.7
  provider: ^6.0.5
  responsive_framework: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
